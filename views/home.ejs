<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>zyp.io</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap"
      rel="stylesheet"
    />
    <style>
      :root {
        --bg-start: #0ea5e9; /* sky-500 */
        --bg-end: #1e3a8a; /* indigo-900 */
        --glass-bg: rgba(255, 255, 255, 0.15);
        --glass-stroke: rgba(255, 255, 255, 0.35);
        --text: #ffffff;
        --btn-bg: #ffffff;
        --btn-text: #0f172a; /* slate-900 */
        --placeholder: rgba(255, 255, 255, 0.75);
        --ring: rgba(255, 255, 255, 0.7);
      }
      * {
        box-sizing: border-box;
      }
      html,
      body {
        height: 100%;
      }
      body {
        margin: 0;
        font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto,
          "Helvetica Neue", Arial, "Noto Sans", "Apple Color Emoji",
          "Segoe UI Emoji";
        color: var(--text);
        background: radial-gradient(
            1200px 800px at 10% 10%,
            rgba(255, 255, 255, 0.15),
            transparent 60%
          ),
          radial-gradient(
            900px 700px at 90% 90%,
            rgba(255, 255, 255, 0.1),
            transparent 60%
          ),
          linear-gradient(135deg, var(--bg-start), var(--bg-end));
        min-height: 100vh;
        display: grid;
        place-items: center;
        overflow: hidden;
      }

      .card {
        width: min(92vw, 560px);
        background: var(--glass-bg);
        border: 1px solid var(--glass-stroke);
        border-radius: 28px;
        padding: 36px;
        backdrop-filter: blur(10px) saturate(140%);
        -webkit-backdrop-filter: blur(10px) saturate(140%);
        box-shadow: 0 20px 50px rgba(0, 0, 0, 0.25),
          inset 0 1px 0 rgba(255, 255, 255, 0.2);
      }

      h1 {
        margin: 0 0 18px;
        font-size: clamp(1.25rem, 1.2rem + 2vw, 2rem);
        font-weight: 600;
        letter-spacing: -0.01em;
        text-shadow: 0 1px 0 rgba(0, 0, 0, 0.15);
      }

      p.lead {
        margin: 0 0 22px;
        font-size: 0.975rem;
        line-height: 1.5;
        color: rgba(255, 255, 255, 0.9);
      }

      .input-wrap {
        position: relative;
      }

      .input {
        width: 100%;
        height: 56px;
        padding: 0 120px 0 18px; /* room for button on the right */
        border-radius: 999px;
        border: 1px solid rgba(255, 255, 255, 0.55);
        background: rgba(255, 255, 255, 0.12);
        color: #fff;
        font-size: 1rem;
        outline: none;
        box-shadow: inset 0 2px 6px rgba(0, 0, 0, 0.15);
        transition: box-shadow 0.2s ease, border-color 0.2s ease,
          background 0.2s ease;
      }

      .input::placeholder {
        color: var(--placeholder);
      }

      .input:focus {
        border-color: var(--ring);
        box-shadow: 0 0 0 4px rgba(255, 255, 255, 0.15),
          inset 0 2px 6px rgba(0, 0, 0, 0.2);
        background: rgba(255, 255, 255, 0.16);
      }

      .submit-btn {
        position: absolute;
        right: 6px;
        top: 6px;
        text-decoration: none;
        height: 44px;
        padding: 0 18px 0 14px;
        border: none;
        border-radius: 999px;
        background: var(--btn-bg);
        color: var(--btn-text);
        font-weight: 600;
        font-size: 0.95rem;
        cursor: pointer;
        display: inline-flex;
        align-items: center;
        gap: 10px;
        box-shadow: 0 8px 20px rgba(15, 23, 42, 0.25);
        transition: transform 0.06s ease, box-shadow 0.2s ease;
      }

      .submit-btn:focus-visible {
        outline: 3px solid rgba(255, 255, 255, 0.75);
        outline-offset: 2px;
      }

      .submit-btn:active {
        transform: translateY(1px);
        box-shadow: 0 6px 16px rgba(15, 23, 42, 0.35);
      }

      .arrow {
        width: 18px;
        height: 18px;
        display: inline-block;
      }

      footer {
        position: fixed;
        left: 50%;
        bottom: 18px;
        transform: translateX(-50%);
        font-size: 0.85rem;
        color: rgba(255, 255, 255, 0.8);
        text-align: center;
        user-select: none;
      }

      /* Subtle floating bubbles for extra polish */
      .bubbles {
        position: absolute;
        inset: 0;
        pointer-events: none;
        overflow: hidden;
      }
      .bubble {
        position: absolute;
        border-radius: 999px;
        opacity: 0.25;
        background: radial-gradient(
          circle at 30% 30%,
          rgba(255, 255, 255, 0.9),
          rgba(255, 255, 255, 0.1) 60%
        );
        filter: blur(1px);
        animation: floaty linear infinite;
      }
      .bubble:nth-child(1) {
        width: 120px;
        height: 120px;
        left: 10%;
        bottom: -120px;
        animation-duration: 20s;
      }
      .bubble:nth-child(2) {
        width: 80px;
        height: 80px;
        left: 60%;
        bottom: -80px;
        animation-duration: 16s;
        animation-delay: 4s;
      }
      .bubble:nth-child(3) {
        width: 150px;
        height: 150px;
        left: 80%;
        bottom: -150px;
        animation-duration: 22s;
        animation-delay: 2s;
      }
      .bubble:nth-child(4) {
        width: 60px;
        height: 60px;
        left: 35%;
        bottom: -60px;
        animation-duration: 14s;
        animation-delay: 1s;
      }

      @keyframes floaty {
        0% {
          transform: translateY(0) translateX(0);
        }
        50% {
          transform: translateY(-40vh) translateX(-10px);
        }
        100% {
          transform: translateY(-80vh) translateX(10px);
        }
      }
      .url-table {
        width: 100%;
        margin-top: 20px;
        border-collapse: collapse;
        background: rgba(255, 255, 255, 0.1);
        border-radius: 12px;
        overflow: hidden;
      }
      .url-table td {
        padding: 12px 16px;
        color: #fff;
        font-size: 0.95rem;
        vertical-align: middle;
      }
      .url-cell a {
        color: #fff;
        text-decoration: none;
        word-break: break-all;
      }
      .copy-cell {
        width: 50px;
        text-align: center;
      }
      .copy-btn {
        color: #fff;
        border: none;
        padding: 2px;
        border-radius: 6px;
        font-size: 1rem;
        cursor: pointer;
        transition: background 0.2s ease;
      }
      #copied {
        display: none;
      }
    </style>
  </head>
  <body>
    <div class="bubbles" aria-hidden="true">
      <span class="bubble"></span>
      <span class="bubble"></span>
      <span class="bubble"></span>
      <span class="bubble"></span>
    </div>

    <main class="card" role="main">
      <h1>zyp.io</h1>
      <p class="lead">
        Shorten your long urls instantly and share them with ease.
      </p>
      <form
        action="/url"
        method="POST"
        aria-label="Single input demo"
        autocomplete="off"
      >
        <div class="input-wrap">
          <label for="query" class="visually-hidden">Your input</label>
          <input
            id="query"
            name="url"
            type="text"
            class="input"
            placeholder="https://yourtoolongurltoread.com"
            required
          />
          <button class="submit-btn" type="submit" aria-label="Submit">
            Submit
            <svg
              class="arrow"
              viewBox="0 0 24 24"
              fill="none"
              xmlns="http://www.w3.org/2000/svg"
              aria-hidden="true"
            >
              <path
                d="M5 12h14M13 5l7 7-7 7"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
              />
            </svg>
          </button>
        </div>
        <a
          href="/analytics"
          class="submit-btn"
          type="submit"
          aria-label="checkAnalytics"
        >
          View Analytics
        </a>
      </form>
      <% if (locals.id) { %>
      <table class="url-table">
        <tr>
          <td class="url-cell">
            <a href="http://localhost:8000/url/<%= id %>" target="_blank">
              http://zyp.io/<%= id %>
            </a>
          </td>
          <td class="copy-cell">
            <svg
              id="copy"
              class="copy-btn"
              onclick="copyToClipboard('http://localhost:8000/url/<%= id %>')"
              aria-hidden="true"
              xmlns="http://www.w3.org/2000/svg"
              width="30"
              height="30"
              fill="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                fill-rule="evenodd"
                d="M18 3a2 2 0 0 1 2 2v10a2 2 0 0 1-2 2h-1V9a4 4 0 0 0-4-4h-3a1.99 1.99 0 0 0-1 .267V5a2 2 0 0 1 2-2h7Z"
                clip-rule="evenodd"
              />
              <path
                fill-rule="evenodd"
                d="M8 7.054V11H4.2a2 2 0 0 1 .281-.432l2.46-2.87A2 2 0 0 1 8 7.054ZM10 7v4a2 2 0 0 1-2 2H4v6a2 2 0 0 0 2 2h7a2 2 0 0 0 2-2V9a2 2 0 0 0-2-2h-3Z"
                clip-rule="evenodd"
              />
            </svg>

            <svg
              id="copied"
              class="copy-btn"
              width="30"
              height="30"
              viewBox="0 -0.5 25 25"
              fill="none"
              xmlns="http://www.w3.org/2000/svg"
            >
              <path
                d="M5.5 12.5L10.167 17L19.5 8"
                stroke="#fff"
                stroke-width="1.5"
                stroke-linecap="round"
                stroke-linejoin="round"
              />
            </svg>
          </td>
        </tr>
      </table>
      <% } %>
    </main>

    <footer>Built with 💖 by Umar Khursheed</footer>

    <style>
      /* helper to visually hide the label but keep it accessible */
      .visually-hidden {
        position: absolute !important;
        height: 1px;
        width: 1px;
        overflow: hidden;
        clip: rect(1px, 1px, 1px, 1px);
        white-space: nowrap;
      }
    </style>
    <script>
      const copied = document.getElementById("copied");
      const copy = document.getElementById("copy");
      function copyToClipboard(text) {
        navigator.clipboard.writeText(text).then(() => {
          copy.style.display = "none";
          copied.style.display = "block";
          setTimeout(() => {
            copied.style.display = "none";
            copy.style.display = "block";
          }, 1000);
        });
      }
    </script>
  </body>
</html>
